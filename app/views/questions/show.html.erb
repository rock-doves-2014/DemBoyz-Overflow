<%= @question.title %>
<%= @question.content %>
<ul>
<% @question.answers.each do |answer| %>
  <li>
    <%= answer.content %>
  </li>
</ul>
<% end %>

<%= button_to "Edit Question", edit_question_path(@question.id) %>
<%= button_to "Delete Question", @question, :method => :edit %>